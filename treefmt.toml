[formatter.nix]
command = "nixfmt"
includes = ["*.nix"]

[formatter.tofu]
command = "tofu"
options = ["fmt", "-"]
includes = ["*.tf"]

[formatter.just]
command = "just"
options = ["--fmt", "--justfile"]
includes = ["justfile", "**/justfile", "*.just"]
no-positional-arg-support = true
